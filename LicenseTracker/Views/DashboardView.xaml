<UserControl x:Class="LicenseTracker.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LicenseTracker.Views"
             xmlns:card="clr-namespace:LicenseTracker.UIComponents.Cards"
             mc:Ignorable="d" 
             d:DesignHeight="950" d:DesignWidth="1500">
    
    <Grid>
        <Border
            Background="{StaticResource ViewBackground}"
            Padding="10">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="103"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <!-- #Tool bar -->
                <Border Grid.Row="0">
                    <Border.Resources>
                        <Style TargetType="Border">
                            <Setter Property="Background" Value="{StaticResource LayerFillColorBrush1}"/>
                            <Setter Property="Margin" Value="0,10"/>
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="CornerRadius" Value="4"/>
                        </Style>
                    </Border.Resources>
                    <StackPanel Orientation="Horizontal">
                        <StackPanel.Resources>
                            <Style TargetType="Button" BasedOn="{StaticResource ToolBarButtonStyle}"/>
                        </StackPanel.Resources>
                        <Button Content="Button1"/>
                        <Button Content="Button2"/>
                    </StackPanel>
                </Border>
                
                <!-- # Notification Area -->
                <Border Grid.Row="1">
                    <StackPanel>
                        <Border>
                            <card:NotificationCard/>
                        </Border>
                    </StackPanel>
                </Border>
                
                <!-- #Search & Filter Area -->
                <Border Grid.Row="2">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <Label Content="Search box 1"/>
                            <TextBox Text="Search1"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1">
                            <Label Content="Filter box 2"/>
                            <TextBox Text="Filter1"/>
                        </StackPanel>

                        <StackPanel Grid.Column="2">
                            <Label Content="Search box 2"/>
                            <TextBox Text="Search2"/>
                        </StackPanel>
                    </Grid>
                </Border>
                
                <!-- #License Item Area -->
                <Border Grid.Row="3">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="0.7*"/>
                            <ColumnDefinition Width="0.3*"/>
                        </Grid.ColumnDefinitions>
                        
                        <!-- #License Item List -->
                        <Border Grid.Column="0">
                            <ListView>
                                <ListViewItem>
                                    I'm a license item!
                                </ListViewItem>
                            </ListView>
                        </Border>
                        
                        <!-- #Info Area -->
                        <Border Grid.Column="1">
                            <StackPanel>
                                <Label Content="This is"/>
                                <Label Content="going to be"/>
                                <Label Content="information"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </Border>
            </Grid>

        </Border> 
    </Grid>
    
</UserControl>
