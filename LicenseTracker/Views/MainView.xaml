<Window x:Class="LicenseTracker.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LicenseTracker.Views"
        xmlns:ui="clr-namespace:LicenseTracker.UIComponents"
        xmlns:vm="clr-namespace:LicenseTracker.ViewModels"
        mc:Ignorable="d"
        Title="License Tracker"
        Height="950" Width="1500"
        WindowStartupLocation="CenterScreen">
    
    <Grid>
        <Border>

            <ContentControl Content="{Binding CurrentContentViewModel}">
                <ContentControl.Resources>
                    <DataTemplate DataType="{x:Type vm:LayoutViewModel}">
                        <ui:Layout/>
                    </DataTemplate>
                </ContentControl.Resources>
            </ContentControl>

        </Border>
    </Grid>
    
</Window>
